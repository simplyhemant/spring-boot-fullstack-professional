server.error.include-message=always
server.error.include-binding-errors=always

# --- Supabase Database Configuration ---
spring.datasource.url=jdbc:postgresql://aws-1-ap-southeast-2.pooler.supabase.com:5432/postgres
spring.datasource.username=postgres.paofaihykhxnodncmzhk
spring.datasource.password=+/92%hghwy3V$bV
spring.datasource.driver-class-name=org.postgresql.Driver

# --- HikariCP Connection Pooling Optimization ---
# Since you are using Supabase's Session Pooler, optimizing Hikari settings prevents connection leaks.
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.max-lifetime=1800000
spring.datasource.hikari.connection-timeout=20000

# --- Hibernate / JPA Settings ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Redis configuration
spring.redis.host=localhost
spring.redis.port=6379
spring.redis.timeout=1000
spring.redis.connect-timeout=1000
spring.redis.lettuce.pool.max-active=8
spring.redis.lettuce.pool.max-idle=8
spring.redis.lettuce.pool.min-idle=0
spring.redis.lettuce.pool.max-wait=1000

# CORS settings
app.cors.allowed-origins=http://localhost:3000

# Pagination settings
spring.data.web.pageable.default-page-size=20
spring.data.web.pageable.max-page-size=100